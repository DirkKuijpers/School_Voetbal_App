<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SchoolVoetbal_App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SchoolVoetbal_App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SchoolVoetbalApp">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#1E1E1E">

            <Button Margin="10" Height="70" HorizontalAlignment="Stretch" Click="Home_Click">
                <StackPanel>
                    <TextBlock Text="🏠" FontSize="20" HorizontalAlignment="Center"/>
                    <TextBlock Text="Home" FontSize="10" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Margin="10" Height="70" HorizontalAlignment="Stretch" Click="Matches_Click">
                <StackPanel>
                    <TextBlock Text="📋" FontSize="20" HorizontalAlignment="Center"/>
                    <TextBlock Text="Matches" FontSize="10" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <StackPanel Height="300"/>

            <TextBlock x:Name="SaldoText"
                       Text="€50"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="10"/>

            <Button Margin="10" Height="70" HorizontalAlignment="Stretch" Click="Profiel_Click">
                <StackPanel>
                    <TextBlock Text="👤" FontSize="20" HorizontalAlignment="Center"/>
                    <TextBlock Text="Profiel" FontSize="10" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

        </StackPanel>

        <Grid Grid.Column="1">
            <Frame x:Name="MainFrame"/>
        </Grid>

    </Grid>

</Window>
