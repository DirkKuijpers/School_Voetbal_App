<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SchoolVoetbalApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SchoolVoetbalApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SchoolVoetbalApp">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

        <Grid.ColumnDefinitions>
            <!-- Sidebar ruimte voor tekst -->
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0"
                Background="#121212"
                CornerRadius="0,20,20,0">

            <StackPanel>
                <Border Background="Transparent" Padding="12" HorizontalAlignment="Center">
                    <Grid Width="80" Height="80">
                        <Image x:Name="LogoImage" Width="80" Height="80" Stretch="Uniform"/>
                        <Grid x:Name="LogoFallback" Visibility="Collapsed">
                            <Ellipse Fill="#2E7D32" Stroke="#1B5E20" StrokeThickness="2" />
                            <TextBlock Text="SV" Foreground="White" FontWeight="Bold" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Nav -->
                <Button x:Name="HomeButton" Margin="12,12,12,6" Height="52" Click="Home_Click" Background="#1B5E20" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left" Padding="14,0">
                    <TextBlock Text="Home" FontSize="16"/>
                </Button>

                <Button Margin="12,6" Height="52" Click="Matches_Click" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left" Padding="14,0">
                    <TextBlock Text="Wedstrijden" FontSize="16"/>
                </Button>

                <Button Margin="12,6" Height="52" Click="Stand_Click" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left" Padding="14,0">
                    <TextBlock Text="Stand" FontSize="16"/>
                </Button>

                <StackPanel Height="280"/>

                <!-- Saldo-->
                <Border Background="#2E7D32"
                        CornerRadius="10"
                        Margin="10"
                        Padding="8">

                    <StackPanel>
                        <TextBlock Text="Saldo" Foreground="White" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="SaldoText"
                                   Text="€0"
                                   Foreground="White"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock x:Name="UsernameText" Text="Niet ingelogd" Foreground="White" FontSize="12" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Profiel -->
                <Button Margin="12,6" Height="52" Click="Profiel_Click" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left" Padding="14,0">
                    <TextBlock Text="Profiel" FontSize="16"/>
                </Button>

            </StackPanel>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Column="1" Background="#F4F4F4">
            <Frame x:Name="MainFrame"/>
        </Grid>

    </Grid>

</Window>
