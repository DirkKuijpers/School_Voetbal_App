<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SchoolVoetbalApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SchoolVoetbalApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SchoolVoetbalApp">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <Border Grid.Column="0"
                Background="#1E1E1E"
                CornerRadius="0,20,20,0">

            <StackPanel>

                <!-- HOME -->
                <Button Margin="10" Height="70" Click="Home_Click">
                    <StackPanel>
                        <TextBlock Text="🏠" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Home" FontSize="10" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- WEDSTRIJDEN -->
                <Button Margin="10" Height="70" Click="Matches_Click">
                    <StackPanel>
                        <TextBlock Text="📋" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Wedstrijden" FontSize="8" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- STAND -->
                <Button Margin="10" Height="70" Click="Stand_Click">
                    <StackPanel>
                        <TextBlock Text="🏆" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Stand" FontSize="10" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <StackPanel Height="280"/>

                <!-- SALDO -->
                <Border Background="#2E7D32"
                        CornerRadius="10"
                        Margin="10"
                        Padding="8">

                    <TextBlock x:Name="SaldoText"
                               Text="€0"
                               Foreground="White"
                               FontSize="18"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                </Border>

                <!-- PROFIEL -->
                <Button Margin="10" Height="70" Click="Profiel_Click">
                    <StackPanel>
                        <TextBlock Text="👤" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Profiel" FontSize="10" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </StackPanel>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Column="1" Background="#F4F4F4">
            <Frame x:Name="MainFrame"/>
        </Grid>

    </Grid>

</Window>
