<Page
    x:Class="SchoolVoetbalApp.WedstrijdPagina"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SchoolVoetbalApp"
    xmlns:models="using:SchoolVoetbalApp.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid Padding="20">
        <StackPanel Spacing="18" HorizontalAlignment="Center">
            <TextBlock Text="Aankomende Wedstrijden" FontSize="26" Foreground="#1B5E20" FontWeight="SemiBold" />

            <ItemsControl x:Name="MatchesList" HorizontalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <controls:ItemsWrapGrid MaximumRowsOrColumns="3" Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:MatchView">
                        <Border Background="White" CornerRadius="15" Padding="14" Margin="10" Height="180" Width="320" PointerEntered="Card_PointerEntered" PointerExited="Card_PointerExited" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>
                            <StackPanel Spacing="10" VerticalAlignment="Stretch">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
                                    <TextBlock Text="{x:Bind HomeTeamName}" FontSize="16" FontWeight="SemiBold" Foreground="#1B5E20" TextAlignment="Center" />
                                    <TextBlock Text="vs" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    <TextBlock Text="{x:Bind AwayTeamName}" FontSize="16" FontWeight="SemiBold" Foreground="#1B5E20" TextAlignment="Center" />
                                </StackPanel>
                                <TextBlock Text="{x:Bind Date}" FontSize="13" Foreground="#666" HorizontalAlignment="Center" />
                                <TextBlock Text="{x:Bind Odds}" FontSize="14" Foreground="#2E7D32" FontWeight="SemiBold" HorizontalAlignment="Center" />
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                                    <Button Content="Bekijk Wedstrijd" Click="ViewMatch_Click" Tag="{x:Bind}" Background="#2E7D32" Foreground="White" BorderThickness="0" Padding="10,6"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Recente Uitslagen" FontSize="22" Foreground="#1B5E20" FontWeight="SemiBold" Margin="0,18,0,0" />

            <ItemsControl x:Name="ResultsGrid" HorizontalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <controls:ItemsWrapGrid MaximumRowsOrColumns="3" Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="15" Padding="14" Margin="10" Height="160" Width="320" PointerEntered="Card_PointerEntered" PointerExited="Card_PointerExited" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>
                            <StackPanel Spacing="6">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                                    <TextBlock Text="{Binding HomeTeam}" FontSize="16" FontWeight="SemiBold" Foreground="#1B5E20" TextAlignment="Center" />
                                    <TextBlock Text="vs" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding AwayTeam}" FontSize="16" FontWeight="SemiBold" Foreground="#1B5E20" TextAlignment="Center" />
                                </StackPanel>
                                <TextBlock Text="{Binding Score}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" />
                                <TextBlock Text="Gespeeld" FontSize="12" Foreground="#666" HorizontalAlignment="Center" />
                                <TextBlock Text="{Binding Date}" FontSize="12" Foreground="#666" HorizontalAlignment="Center" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </Grid>
</Page>
